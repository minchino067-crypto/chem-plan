/* ------------------------------------------------------------------
   端末間の同期を使うときだけ、ここを埋めます。
   空のままでも、アプリはこの端末のブラウザに保存して普通に動きます。

   手順は README.md の「同期のセットアップ」を見てください。
   Firebase コンソールで Web アプリを登録すると表示される firebaseConfig を
   そのまま貼り付けます。この値はブラウザに配られる前提の公開設定で、
   秘密鍵ではありません。実際の防御は Firestore のルールで行います。
   ------------------------------------------------------------------ */
window.FIREBASE_CONFIG = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: ""
};
